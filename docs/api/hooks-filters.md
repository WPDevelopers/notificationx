# Hooks & Filters

> **Status:** stub — outline only. Grep `do_action(` / `apply_filters(` across `includes/` to enumerate.

The public extensibility surface — how themes, other plugins, and especially `notificationx-pro` integrate.

## Actions
_TODO — table of key actions, when they fire, args._

## Filters
_TODO — table of key filters, what they modify, args. Include the Pro integration points such as `nx_pro_alert_popup`._

## How Pro hooks in
_TODO — Pro features live in the separate `notificationx-pro` plugin and integrate via the same Extension system + these filters/actions. Don't add Pro-only logic to the free repo._

## Source
- Grep `includes/` for `do_action(` and `apply_filters(`.
- [../../CLAUDE.md](../../CLAUDE.md) — conventions.
